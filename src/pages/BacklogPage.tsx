import React from 'react';

const BacklogPage: React.FC = () => {
    return (
        <div>
            <h1>Backlog Page</h1>
            <p>View and manage your backlog here.</p>
        </div>
    );
};

export default BacklogPage;
